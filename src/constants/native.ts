/**
 * Object Methods
 */
export const {
  create,
  assign,
  is,
  keys,
  values,
  defineProperty,
  getOwnPropertyNames
} = Object;

/**
 * Array from
 */
export const {
  from,
  isArray
} = Array;
