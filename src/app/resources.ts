
export function connect (dom: Document) {

  document.head.querySelectorAll('script').forEach(script => {

  });

  document.head.querySelectorAll('style').forEach(script => {

  });

  document.head.querySelectorAll('link').forEach(script => {

  });

}
